# Environment variables config
PROJECT_NAME="VQA ViT-PubMedBERT API Production"
VERSION="1.0.0"

# Change this to 'cuda' if running on a GPU machine
DEVICE="cpu" 

# Thresholds
MAX_IMAGE_SIZE_MB=5
MAX_QUESTION_LENGTH=128

# File paths
MODEL_PATH="models/best_vit_pubmedbert_slake.pth"